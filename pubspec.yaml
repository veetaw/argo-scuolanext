name: registro_elettronico
description: Applicazione non ufficiale per il registro elettronico argo

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  cupertino_icons: ^0.1.0

dev_dependencies:
  intl_translation: ^0.16.6
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
