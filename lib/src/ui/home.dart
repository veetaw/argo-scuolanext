import 'package:flutter/material.dart';

class Home extends StatelessWidget {
  static String get routeName => "home";
  @override
  Widget build(BuildContext context) {
    return new Scaffold(body: new Container() // todo
        );
  }
}
